export function DANLogo({ className }: { className?: string }) {
  return (
    <img
      className={className}
      src="/logo.png"
      alt="DAN Placement Prep"
      loading="lazy"
    />
  );
}
