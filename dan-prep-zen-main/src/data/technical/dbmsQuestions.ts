export const dbmsQuestions = [
  { question: 'What is the difference between clustered and non-clustered index?', options: ['No difference', 'Clustered reorders actual data rows; non-clustered creates separate pointer structure', 'Non-clustered is faster', 'Clustered uses more memory'], correct: 1, explanation: 'A clustered index sorts and stores data rows physically in order (one per table). Non-clustered index creates a separate structure with pointers to data, allowing multiple per table.' },
  { question: 'What are the four properties of a transaction (ACID) and why is each important?', options: ['Speed, Security, Scalability, Simplicity', 'Atomicity, Consistency, Isolation, Durability', 'Access, Control, Identity, Data', 'Authentication, Caching, Indexing, Distribution'], correct: 1, explanation: 'Atomicity: all or nothing. Consistency: valid state transitions. Isolation: concurrent transactions don\'t interfere. Durability: committed changes survive crashes.' },
  { question: 'What is a correlated subquery and how does it differ from a regular subquery?', options: ['Same thing', 'Correlated subquery references outer query and executes once per outer row', 'Correlated is faster', 'Regular subquery runs multiple times'], correct: 1, explanation: 'A correlated subquery references columns from the outer query and is re-evaluated for each row of the outer query, making it potentially slower but necessary for row-by-row comparisons.' },
  { question: 'What is the difference between WHERE and HAVING clause?', options: ['No difference', 'WHERE filters rows before grouping; HAVING filters groups after aggregation', 'HAVING comes first', 'WHERE works with aggregates'], correct: 1, explanation: 'WHERE filters individual rows before GROUP BY is applied. HAVING filters grouped results after aggregation. HAVING can use aggregate functions (COUNT, SUM) while WHERE cannot.' },
  { question: 'Explain the difference between UNION and UNION ALL.', options: ['Same thing', 'UNION removes duplicates; UNION ALL keeps all rows including duplicates', 'UNION ALL removes duplicates', 'UNION is faster'], correct: 1, explanation: 'UNION combines results and removes duplicate rows (requires sorting). UNION ALL combines all rows including duplicates and is faster since it skips the duplicate elimination step.' },
  { question: 'What is a deadlock in DBMS and how can it be prevented?', options: ['Database crash', 'Circular wait between transactions; prevented by timeout, ordering, or detection algorithms', 'Memory overflow', 'Index corruption'], correct: 1, explanation: 'Deadlock occurs when transactions form a circular wait for locks. Prevention strategies: lock ordering, timeout-based abort, wait-die/wound-wait schemes, or deadlock detection with rollback.' },
  { question: 'What is the difference between DELETE, TRUNCATE, and DROP?', options: ['All remove data the same way', 'DELETE removes rows (logged/rollback), TRUNCATE removes all rows (minimal log), DROP removes table entirely', 'TRUNCATE is same as DROP', 'DELETE removes table'], correct: 1, explanation: 'DELETE: removes specific/all rows, logged, can rollback, triggers fire. TRUNCATE: removes all rows, minimal logging, resets identity, no triggers. DROP: removes entire table structure and data.' },
  { question: 'What is a B+ Tree index and why is it preferred for databases?', options: ['Binary tree', 'Balanced tree where all data is in leaf nodes linked together, enabling efficient range queries', 'Hash-based structure', 'Linked list'], correct: 1, explanation: 'B+ Tree keeps all data pointers in leaf nodes (linked for range scans) with internal nodes for routing. It provides O(log n) lookups, efficient range queries, and stays balanced during insertions.' },
  { question: 'What is database sharding?', options: ['Data compression', 'Horizontal partitioning of data across multiple database instances', 'Data encryption', 'Data backup'], correct: 1, explanation: 'Sharding distributes data across multiple database servers (shards) based on a shard key. Each shard holds a subset of data, enabling horizontal scaling for large datasets.' },
  { question: 'What is the difference between optimistic and pessimistic concurrency control?', options: ['Same approach', 'Optimistic allows concurrent access and checks conflicts at commit; pessimistic locks resources upfront', 'Optimistic uses locks', 'Pessimistic never locks'], correct: 1, explanation: 'Pessimistic locking acquires locks before accessing data, preventing conflicts but reducing concurrency. Optimistic locking allows concurrent access and validates at commit time, aborting if conflicts exist.' },
  { question: 'What is a materialized view and how does it differ from a regular view?', options: ['Same thing', 'Materialized view stores query results physically; regular view executes query each time', 'Regular view stores data', 'Materialized view is virtual'], correct: 1, explanation: 'A regular view is a virtual table that runs its query each time accessed. A materialized view stores the result set physically, improving read performance but requiring periodic refresh.' },
  { question: 'What are the different types of joins and when would you use each?', options: ['Only INNER JOIN exists', 'INNER (matching rows), LEFT (all left + matching right), RIGHT (all right + matching left), FULL (all from both), CROSS (cartesian product)', 'Only two types exist', 'Joins are same as subqueries'], correct: 1, explanation: 'Use INNER for matching records only, LEFT/RIGHT for all records from one table with matches from another, FULL for all records from both, CROSS for all combinations (cartesian product).' },
  { question: 'What is a covering index?', options: ['Index that covers the table', 'Index that contains all columns needed by a query, avoiding table lookup', 'Primary key index', 'Full-text index'], correct: 1, explanation: 'A covering index includes all columns referenced in a query (SELECT, WHERE, JOIN). The query can be satisfied entirely from the index without accessing the base table, significantly improving performance.' },
  { question: 'What is the Write-Ahead Logging (WAL) protocol?', options: ['Writing data first', 'All modifications are written to log before being applied to database, ensuring durability', 'Writing after commit', 'No logging protocol'], correct: 1, explanation: 'WAL requires that log records be written to stable storage before the corresponding data modifications. This ensures that committed transactions can be recovered after crashes (durability).' },
  { question: 'What is the difference between 3NF and BCNF?', options: ['Same thing', '3NF allows transitive dependency if determinant is a candidate key; BCNF requires every determinant to be a superkey', 'BCNF is weaker than 3NF', '3NF is stricter'], correct: 1, explanation: '3NF: no non-prime attribute transitively depends on candidate key. BCNF is stricter: every determinant must be a superkey. A relation in BCNF is always in 3NF, but not vice versa.' },
  { question: 'What is a phantom read and which isolation level prevents it?', options: ['Reading deleted data; READ COMMITTED', 'New rows appearing between reads in same transaction; SERIALIZABLE', 'Reading uncommitted data; READ UNCOMMITTED', 'Duplicate reads; REPEATABLE READ'], correct: 1, explanation: 'Phantom read: a transaction re-executes a query and finds new rows inserted by another committed transaction. Only SERIALIZABLE isolation level fully prevents phantom reads.' },
  { question: 'What is database normalization and denormalization trade-off?', options: ['Always normalize', 'Normalization reduces redundancy but increases joins; denormalization improves read performance but increases redundancy', 'Always denormalize', 'No trade-off exists'], correct: 1, explanation: 'Normalization eliminates redundancy and anomalies but requires more joins for queries. Denormalization adds controlled redundancy to reduce joins and improve read performance, common in data warehouses.' },
  { question: 'What is a transaction log and why is it important?', options: ['Error log', 'Sequential record of all database modifications used for recovery and rollback', 'Query log', 'Access log'], correct: 1, explanation: 'Transaction log records all modifications (before and after images) sequentially. It enables crash recovery (redo committed, undo uncommitted), point-in-time recovery, and replication.' },
  { question: 'What is the difference between horizontal and vertical partitioning?', options: ['Same thing', 'Horizontal splits rows across tables; vertical splits columns across tables', 'Vertical splits rows', 'Horizontal splits columns'], correct: 1, explanation: 'Horizontal partitioning distributes rows across multiple tables (e.g., by date range). Vertical partitioning splits columns into separate tables, keeping frequently accessed columns together.' },
  { question: 'What is a stored procedure and what are its advantages over inline SQL?', options: ['Same as inline SQL', 'Precompiled SQL logic stored in DB; faster execution, reduced network traffic, better security', 'Stored procedures are slower', 'No advantages'], correct: 1, explanation: 'Stored procedures are precompiled, reducing parsing overhead. They reduce network traffic (single call vs multiple queries), provide security through parameterization, and centralize business logic.' },
  { question: 'What is the CAP theorem in distributed databases?', options: ['Database capacity limit', 'A distributed system can guarantee only two of Consistency, Availability, and Partition tolerance', 'Caching algorithm', 'Compression protocol'], correct: 1, explanation: 'CAP theorem states that in a distributed system, you can only guarantee two of three: Consistency (all nodes see same data), Availability (system responds), Partition tolerance (works despite network splits).' },
  { question: 'What is an execution plan and how do you optimize queries using it?', options: ['Project timeline', 'Database optimizer\'s step-by-step strategy for executing a query; analyze for table scans and missing indexes', 'Code execution order', 'Memory plan'], correct: 1, explanation: 'Execution plan shows how the DB engine processes a query (joins, scans, index usage). Look for full table scans, missing indexes, expensive sorts, and nested loops on large tables.' },
  { question: 'What is the difference between OLTP and OLAP?', options: ['Same thing', 'OLTP handles real-time transactions (normalized); OLAP handles analytical queries (denormalized)', 'OLAP is for transactions', 'OLTP is for analysis'], correct: 1, explanation: 'OLTP: optimized for fast INSERT/UPDATE/DELETE with normalized schema (e.g., banking). OLAP: optimized for complex analytical queries with denormalized/star schema (e.g., business intelligence).' },
  { question: 'What is a composite index and what is the "leftmost prefix" rule?', options: ['Index on one column', 'Index on multiple columns; queries must use leftmost columns in order for index to be utilized', 'Index on all columns', 'Prefix-based text search'], correct: 1, explanation: 'A composite index covers multiple columns. The leftmost prefix rule means the index is only used if the query filters on columns starting from the left. Index on (A,B,C) helps WHERE A=x or WHERE A=x AND B=y but not WHERE B=y alone.' },
  { question: 'What are SQL injection attacks and how to prevent them?', options: ['Database error', 'Malicious SQL in user input; prevent with parameterized queries, input validation, and least privilege', 'Network attack', 'Hardware failure'], correct: 1, explanation: 'SQL injection inserts malicious SQL through user inputs. Prevention: use parameterized queries/prepared statements, input validation, stored procedures, ORM frameworks, and principle of least privilege.' },
  { question: 'What is eventual consistency in distributed databases?', options: ['Immediate consistency', 'All replicas will converge to same value given enough time without new updates', 'No consistency', 'Random consistency'], correct: 1, explanation: 'Eventual consistency guarantees that if no new updates are made, all replicas will eventually converge to the same value. Used in highly available systems like DynamoDB and Cassandra.' },
  { question: 'What is a trigger and what are the types?', options: ['UI event handler', 'Automatic SQL execution on table events; BEFORE/AFTER triggers on INSERT/UPDATE/DELETE', 'Scheduled job', 'Index type'], correct: 1, explanation: 'Triggers automatically execute SQL when specific events occur on a table. Types: BEFORE (validates/modifies data before operation), AFTER (audit logging, cascading updates), INSTEAD OF (for views).' },
  { question: 'What is the difference between CHAR and VARCHAR data types?', options: ['Same storage', 'CHAR is fixed-length (padded with spaces); VARCHAR is variable-length (stores actual length)', 'VARCHAR uses more space', 'CHAR is variable-length'], correct: 1, explanation: 'CHAR(n) always uses n bytes (right-padded with spaces). VARCHAR(n) uses actual string length + overhead bytes. Use CHAR for fixed-length data (country codes), VARCHAR for variable-length (names).' },
  { question: 'What is a cursor and what are its disadvantages?', options: ['Pointer only', 'Row-by-row processing mechanism; disadvantages include poor performance, high memory usage, and lock contention', 'Always recommended', 'Has no disadvantages'], correct: 1, explanation: 'Cursors process rows one at a time instead of set-based operations. They are slow (row-by-row vs set operations), consume memory, hold locks longer, and should be avoided in favor of set-based SQL.' },
  { question: 'What is the difference between INNER JOIN and EXISTS?', options: ['Same performance', 'INNER JOIN returns matching columns from both tables; EXISTS returns true/false for subquery existence check', 'EXISTS is always slower', 'INNER JOIN is for subqueries'], correct: 1, explanation: 'INNER JOIN produces result set with columns from both tables. EXISTS is a semi-join that checks if subquery returns any rows (stops at first match). EXISTS can be faster for existence checks with correlated subqueries.' },
  { question: 'What is a data warehouse star schema?', options: ['Star-shaped database', 'Central fact table connected to dimension tables in a star pattern for analytical queries', 'Schema for OLTP', 'Network topology'], correct: 1, explanation: 'Star schema has a central fact table (containing measures/metrics) connected to denormalized dimension tables (who, what, when, where). Optimized for OLAP queries with simple joins.' },
  { question: 'What is connection pooling and why is it important?', options: ['Swimming pool', 'Reusing database connections instead of creating new ones; reduces overhead and improves performance', 'Network routing', 'Data caching'], correct: 1, explanation: 'Connection pooling maintains a pool of reusable database connections. Creating connections is expensive (TCP handshake, authentication). Pooling reduces this overhead significantly in web applications.' },
  { question: 'What is a self-join and when would you use it?', options: ['Joining with itself is impossible', 'Table joined with itself using aliases; useful for hierarchical data or comparing rows within same table', 'Same as CROSS JOIN', 'Recursive query'], correct: 1, explanation: 'A self-join joins a table to itself using different aliases. Use cases: finding employees and their managers (same employee table), comparing rows, hierarchical queries.' },
  { question: 'What is the difference between a primary key and a unique key?', options: ['Same thing', 'Primary key: no NULLs, one per table, creates clustered index; Unique key: allows one NULL, multiple per table', 'Unique key doesn\'t allow NULLs', 'Primary key allows duplicates'], correct: 1, explanation: 'Primary key uniquely identifies rows, doesn\'t allow NULLs, and there\'s only one per table (usually creates clustered index). Unique key ensures uniqueness but allows one NULL and multiple unique keys per table.' },
  { question: 'What is database replication and its types?', options: ['Data backup', 'Copying data across multiple servers; types include master-slave, master-master, and synchronous/asynchronous', 'Data compression', 'Data encryption'], correct: 1, explanation: 'Replication copies data across servers for availability and read scaling. Master-slave: one write node, multiple read replicas. Master-master: multiple write nodes. Can be synchronous (consistent) or asynchronous (faster).' },
  { question: 'What are window functions in SQL?', options: ['GUI functions', 'Functions that perform calculations across a set of rows related to current row without grouping', 'Same as aggregate functions', 'Operating system functions'], correct: 1, explanation: 'Window functions (ROW_NUMBER, RANK, LAG, LEAD, SUM OVER) compute values across a window of rows related to the current row. Unlike GROUP BY, they don\'t collapse rows into groups.' },
  { question: 'What is the N+1 query problem?', options: ['Query syntax error', 'Executing 1 query for parent + N queries for each child record; solved by eager loading/JOIN', 'Having N+1 tables', 'Index limitation'], correct: 1, explanation: 'N+1 problem: fetching a list (1 query) then querying related data for each item (N queries). Causes performance issues. Solve with JOINs, eager loading, or batch fetching.' },
  { question: 'What is the difference between pessimistic and optimistic locking strategies?', options: ['Same strategy', 'Pessimistic locks rows during read; optimistic uses version numbers and checks at write time', 'Optimistic always locks', 'Pessimistic never locks'], correct: 1, explanation: 'Pessimistic: lock data when reading (SELECT FOR UPDATE), preventing conflicts but reducing concurrency. Optimistic: read without lock, check version/timestamp at update time, retry on conflict.' },
  { question: 'What is a Common Table Expression (CTE) and when to use it?', options: ['CSS expression', 'Named temporary result set within a query; improves readability and enables recursion', 'Cache mechanism', 'Connection type'], correct: 1, explanation: 'WITH clause defines a CTE — a named temporary result set. CTEs improve query readability, enable recursive queries (hierarchical data), and can replace complex subqueries.' },
  { question: 'What is the MVCC (Multi-Version Concurrency Control) mechanism?', options: ['Version control for code', 'Maintains multiple versions of data to allow concurrent reads without blocking writes', 'Memory management', 'File versioning'], correct: 1, explanation: 'MVCC keeps multiple versions of each row. Readers see a consistent snapshot without blocking writers. Used by PostgreSQL, MySQL InnoDB, and Oracle to achieve high concurrency with isolation.' },
  { question: 'What is a database index and when should you NOT create one?', options: ['Always create indexes', 'Data structure for fast lookups; avoid on frequently updated columns, small tables, and low-selectivity columns', 'Never create indexes', 'Indexes have no downsides'], correct: 1, explanation: 'Indexes speed up reads but slow down writes (must update index on INSERT/UPDATE/DELETE). Avoid on: frequently modified columns, small tables, columns with low cardinality, and columns rarely used in WHERE.' },
  { question: 'What is the difference between RANK(), DENSE_RANK(), and ROW_NUMBER()?', options: ['All return same values', 'ROW_NUMBER assigns unique sequential numbers; RANK skips after ties; DENSE_RANK doesn\'t skip', 'Only ROW_NUMBER exists', 'RANK and DENSE_RANK are identical'], correct: 1, explanation: 'For values 10,20,20,30: ROW_NUMBER gives 1,2,3,4 (unique). RANK gives 1,2,2,4 (ties get same rank, next is skipped). DENSE_RANK gives 1,2,2,3 (ties same rank, no skip).' },
  { question: 'What is a foreign key constraint and what are cascading actions?', options: ['Just a reference', 'Enforces referential integrity; CASCADE automatically propagates DELETE/UPDATE to child rows', 'Only prevents inserts', 'Has no cascading options'], correct: 1, explanation: 'Foreign key ensures referential integrity between tables. Cascading actions: CASCADE (auto-delete/update children), SET NULL (set FK to NULL), SET DEFAULT, RESTRICT (prevent if children exist).' },
  { question: 'What is database connection leak and how to prevent it?', options: ['Data leak', 'Connections opened but never closed/returned to pool; prevent with try-with-resources and connection pool monitoring', 'Memory leak only', 'Cannot be prevented'], correct: 1, explanation: 'Connection leak occurs when application opens DB connections but doesn\'t close them, exhausting the connection pool. Prevent with try-with-resources/finally blocks, connection pool timeouts, and monitoring.' },
  { question: 'What is the difference between a subquery and a derived table?', options: ['Same thing', 'Subquery is nested query; derived table is subquery in FROM clause treated as temporary table', 'Derived table is permanent', 'Subquery is faster'], correct: 1, explanation: 'A subquery can appear in SELECT, WHERE, or FROM. When in FROM clause, it\'s called a derived table — it creates a temporary result set that the outer query can reference with an alias.' },
  { question: 'What is query optimization and what factors affect query performance?', options: ['Writing shorter queries', 'DB engine choosing efficient execution path; affected by indexes, statistics, join order, and data distribution', 'Using fewer tables', 'Running queries at night'], correct: 1, explanation: 'Query optimizer analyzes possible execution plans and chooses the most efficient one based on indexes, table statistics, join strategies, data distribution, and available memory.' },
  { question: 'What is the difference between TRUNCATE and DELETE in terms of transaction logging?', options: ['Both log same way', 'DELETE logs each row individually; TRUNCATE logs page deallocation only (minimal logging)', 'TRUNCATE logs more', 'Neither uses logging'], correct: 1, explanation: 'DELETE logs each individual row removal (can be rolled back row by row). TRUNCATE uses minimal logging (logs page deallocations only), making it much faster for removing all rows.' },
  { question: 'What is a hash index and when is it better than B-tree?', options: ['Always better than B-tree', 'Uses hash function for O(1) lookups; better for exact match queries but not for range queries', 'Same as B-tree', 'Never useful'], correct: 1, explanation: 'Hash index uses a hash function mapping keys to buckets for O(1) average lookup. Excellent for exact equality queries (WHERE id = 5) but cannot support range queries, ORDER BY, or partial matches.' },
  { question: 'What is data integrity and what are its types?', options: ['Data backup', 'Accuracy and consistency of data; types: entity (PK), referential (FK), domain (data type/range), user-defined', 'Only one type exists', 'Same as security'], correct: 1, explanation: 'Data integrity ensures accuracy and consistency. Entity integrity: unique PKs. Referential integrity: valid FKs. Domain integrity: valid data types/ranges. User-defined: business rules.' },
  { question: 'What is the difference between CROSS JOIN and INNER JOIN?', options: ['Same operation', 'CROSS JOIN produces cartesian product (all combinations); INNER JOIN only returns matching rows based on condition', 'INNER JOIN has no condition', 'CROSS JOIN filters rows'], correct: 1, explanation: 'CROSS JOIN returns the cartesian product of two tables (every row combined with every row, no condition). INNER JOIN returns only rows where the join condition matches between tables.' },
];
