export const osQuestions = [
  { question: 'What is the difference between a process and a thread in terms of memory and context switching?', options: ['No difference', 'Process has separate memory space and heavier context switch; threads share memory with lighter switch', 'Threads have separate memory', 'Process switching is faster'], correct: 1, explanation: 'Processes have independent memory spaces (code, data, heap, stack) and context switching involves saving/restoring entire address space. Threads share process memory and only need to save/restore registers and stack, making switching faster.' },
  { question: 'Explain the Banker\'s Algorithm. How does it achieve deadlock avoidance?', options: ['Kills processes', 'Simulates resource allocation and checks if system remains in safe state before granting request', 'Prevents all resource sharing', 'Uses timeouts only'], correct: 1, explanation: 'Banker\'s Algorithm checks if granting a resource request keeps the system in a safe state (a sequence exists where all processes can finish). If not safe, the request is denied until resources are available.' },
  { question: 'What are the four necessary conditions for deadlock?', options: ['Locking, Waiting, Running, Sleeping', 'Mutual Exclusion, Hold and Wait, No Preemption, Circular Wait', 'Starvation, Priority, Scheduling, Memory', 'Process, Thread, Memory, CPU'], correct: 1, explanation: 'Deadlock requires all four: Mutual Exclusion (exclusive resource access), Hold and Wait (holding while requesting), No Preemption (cannot forcibly take resources), Circular Wait (circular dependency chain).' },
  { question: 'What is the difference between preemptive and non-preemptive scheduling?', options: ['Same approach', 'Preemptive can interrupt running process; non-preemptive lets process run until completion or voluntary yield', 'Non-preemptive interrupts processes', 'Preemptive is non-interruptible'], correct: 1, explanation: 'Preemptive scheduling can forcibly remove a running process from CPU (e.g., Round Robin, SRTF). Non-preemptive lets the process run until it finishes, blocks, or voluntarily releases CPU (e.g., FCFS, SJF).' },
  { question: 'What is thrashing and how does the working set model prevent it?', options: ['Fast processing', 'Excessive paging due to insufficient frames; working set model allocates frames based on recent page access patterns', 'CPU overheating', 'Disk failure'], correct: 1, explanation: 'Thrashing occurs when processes spend more time paging than executing. The working set model tracks recently used pages and ensures each process gets enough frames for its working set, preventing excessive page faults.' },
  { question: 'Compare FCFS, SJF, SRTF, and Round Robin scheduling algorithms.', options: ['All perform equally', 'FCFS: simple but convoy effect; SJF: optimal avg wait but starvation; SRTF: preemptive SJF; RR: fair time slicing', 'RR is always best', 'FCFS is optimal'], correct: 1, explanation: 'FCFS: simple, no starvation, but convoy effect. SJF: minimum average waiting time but can starve long processes. SRTF: preemptive SJF, better response. RR: fair, good for time-sharing, but performance depends on quantum size.' },
  { question: 'What is the difference between paging and segmentation?', options: ['Same mechanism', 'Paging: fixed-size blocks, no external fragmentation; Segmentation: variable-size logical units, no internal fragmentation', 'Paging has external fragmentation', 'Segmentation uses fixed blocks'], correct: 1, explanation: 'Paging divides memory into fixed-size pages/frames (eliminates external fragmentation, may have internal). Segmentation divides into variable-size logical segments (eliminates internal fragmentation, may have external).' },
  { question: 'What is the difference between internal and external fragmentation?', options: ['Same thing', 'Internal: wasted space within allocated blocks; External: free space scattered in small non-contiguous chunks', 'Internal is worse', 'External happens inside blocks'], correct: 1, explanation: 'Internal fragmentation: allocated block is larger than needed, wasting space inside (occurs with paging). External fragmentation: total free memory is sufficient but scattered in small chunks (occurs with segmentation).' },
  { question: 'What are page replacement algorithms? Compare FIFO, LRU, and Optimal.', options: ['All are identical', 'FIFO: replaces oldest page; LRU: replaces least recently used; Optimal: replaces page not needed for longest time', 'FIFO is optimal', 'LRU replaces newest page'], correct: 1, explanation: 'FIFO: simple but suffers Belady\'s anomaly. LRU: good performance, replaces least recently used page (approximated using counters/stack). Optimal: best but impractical (requires future knowledge).' },
  { question: 'What is Belady\'s Anomaly?', options: ['Normal behavior', 'Increasing frames can increase page faults with FIFO algorithm', 'Decreasing frames reduces faults', 'Occurs with all algorithms'], correct: 1, explanation: 'Belady\'s Anomaly is a counterintuitive phenomenon where increasing the number of page frames can increase page faults. It occurs with FIFO but not with stack-based algorithms like LRU and Optimal.' },
  { question: 'What is a race condition and how to prevent it?', options: ['CPU racing', 'Multiple processes accessing shared data concurrently with unpredictable results; prevent with mutex, semaphore, or monitors', 'Network speed issue', 'Memory error'], correct: 1, explanation: 'Race condition occurs when multiple processes/threads access shared data concurrently, and the outcome depends on execution order. Prevent with synchronization primitives: mutex, semaphore, monitors, or atomic operations.' },
  { question: 'What is the difference between a mutex and a semaphore?', options: ['Same thing', 'Mutex: binary lock owned by a thread; Semaphore: counter allowing N concurrent accesses', 'Mutex allows multiple access', 'Semaphore is binary only'], correct: 1, explanation: 'Mutex: binary lock (0/1), owned by locking thread, only owner can unlock. Semaphore: counter variable, can allow N concurrent accesses (counting semaphore), any thread can signal. Mutex prevents priority inversion.' },
  { question: 'What is priority inversion and how does priority inheritance solve it?', options: ['Normal scheduling', 'Low-priority task holding resource needed by high-priority task; priority inheritance temporarily raises low-priority task', 'High priority waiting for CPU', 'Cannot be solved'], correct: 1, explanation: 'Priority inversion: high-priority task waits for resource held by low-priority task, while medium-priority task preempts the low-priority one. Priority inheritance temporarily boosts low-priority task to prevent this.' },
  { question: 'What is the difference between user-level and kernel-level threads?', options: ['Same implementation', 'User-level: managed by user library, faster switch but no true parallelism; Kernel-level: managed by OS, true parallelism but slower switch', 'User-level is faster for all', 'Kernel threads don\'t exist'], correct: 1, explanation: 'User-level threads are managed by a user library (fast creation/switch, but one blocking call blocks all). Kernel-level threads are managed by OS (true parallelism on multicore, but slower creation/switch).' },
  { question: 'What is virtual memory and how does demand paging work?', options: ['Extra RAM', 'Abstraction providing larger address space than physical memory; demand paging loads pages only when accessed (on page fault)', 'Cache memory', 'ROM extension'], correct: 1, explanation: 'Virtual memory uses disk to extend available memory, giving each process its own address space. Demand paging loads pages into memory only when accessed — a page fault triggers loading from disk.' },
  { question: 'What is the Translation Lookaside Buffer (TLB)?', options: ['Transfer buffer', 'Cache for page table entries that speeds up virtual-to-physical address translation', 'Network buffer', 'Disk buffer'], correct: 1, explanation: 'TLB is a small, fast cache storing recent virtual-to-physical page mappings. Without TLB, every memory access requires page table lookup (slow). TLB hit avoids this overhead, achieving near-direct access speed.' },
  { question: 'What is the difference between cooperative and preemptive multitasking?', options: ['Same thing', 'Cooperative: process voluntarily yields CPU; Preemptive: OS forcibly interrupts processes using timer', 'Cooperative is better', 'Preemptive requires cooperation'], correct: 1, explanation: 'Cooperative: processes voluntarily yield CPU (one misbehaving process can freeze system). Preemptive: OS uses timer interrupts to forcibly switch processes (ensures fairness, used in modern OS).' },
  { question: 'What is the Producer-Consumer problem and its solution?', options: ['Manufacturing issue', 'Synchronization problem where producer adds to buffer and consumer removes; solved with semaphores (empty, full, mutex)', 'Network protocol', 'Memory allocation'], correct: 1, explanation: 'Producer adds items to a bounded buffer, consumer removes them. Use three semaphores: empty (tracks empty slots), full (tracks filled slots), and mutex (ensures mutual exclusion on buffer access).' },
  { question: 'What is the Readers-Writers problem?', options: ['File read/write', 'Multiple readers can read simultaneously but writers need exclusive access; solved with reader-preference or writer-preference', 'Disk scheduling', 'Memory management'], correct: 1, explanation: 'Multiple readers can read shared data concurrently, but a writer needs exclusive access. Solutions: reader-preference (may starve writers), writer-preference (may starve readers), or fair solution using a queue.' },
  { question: 'What is the Dining Philosophers problem?', options: ['Restaurant management', 'Five philosophers sharing forks must avoid deadlock and starvation when eating; classic synchronization problem', 'Database concurrency', 'Network routing'], correct: 1, explanation: 'Five philosophers sit at a table with five forks. Each needs two forks to eat. Solutions: limit simultaneous eaters, asymmetric pickup order, or use a waiter (semaphore) to prevent deadlock.' },
  { question: 'What is a system call and how does the mode switch work?', options: ['Function call', 'Interface for user programs to request OS services; triggers mode switch from user mode to kernel mode via trap/interrupt', 'Library function', 'API call'], correct: 1, explanation: 'System calls are the interface between user programs and OS kernel. When invoked, a trap/software interrupt switches CPU from user mode to kernel mode, executes the service, then returns to user mode.' },
  { question: 'What is the difference between logical and physical address space?', options: ['Same space', 'Logical: addresses generated by CPU (virtual); Physical: actual addresses in RAM; MMU maps between them', 'Physical is virtual', 'Logical is in RAM'], correct: 1, explanation: 'Logical (virtual) addresses are generated by the CPU during program execution. Physical addresses are actual locations in RAM. The Memory Management Unit (MMU) translates logical to physical addresses at runtime.' },
  { question: 'What are the disk scheduling algorithms (FCFS, SSTF, SCAN, C-SCAN)?', options: ['All perform equally', 'FCFS: order of arrival; SSTF: nearest request; SCAN: elevator algorithm; C-SCAN: circular scan for uniform wait', 'Only FCFS exists', 'SCAN is non-preemptive only'], correct: 1, explanation: 'FCFS: simple, fair but high seek time. SSTF: shortest seek, good throughput but can starve far requests. SCAN (elevator): moves one direction then reverses. C-SCAN: returns to start after reaching end, providing more uniform wait times.' },
  { question: 'What is a zombie process and an orphan process?', options: ['Malware types', 'Zombie: terminated but parent hasn\'t read exit status; Orphan: parent terminated first, adopted by init', 'Same thing', 'Cannot occur in modern OS'], correct: 1, explanation: 'Zombie: process finished execution but its entry remains in process table until parent calls wait(). Orphan: parent process terminated before child; the child is adopted by init process (PID 1).' },
  { question: 'What is the difference between monolithic kernel and microkernel?', options: ['Same architecture', 'Monolithic: all OS services in kernel space (fast but large); Microkernel: minimal kernel with services in user space (modular but slower IPC)', 'Microkernel is larger', 'Monolithic is modular'], correct: 1, explanation: 'Monolithic kernel runs all OS services (file system, networking, drivers) in kernel space — fast but hard to maintain. Microkernel keeps minimal services in kernel and runs others in user space — modular but IPC overhead.' },
  { question: 'What is memory-mapped I/O vs port-mapped I/O?', options: ['Same mechanism', 'Memory-mapped: I/O devices share address space with RAM; Port-mapped: separate address space with special instructions', 'Port-mapped shares address space', 'Memory-mapped uses special instructions'], correct: 1, explanation: 'Memory-mapped I/O maps device registers into the same address space as memory (use normal load/store instructions). Port-mapped I/O uses a separate address space with special IN/OUT instructions.' },
  { question: 'What is copy-on-write (COW) optimization?', options: ['Always copies data', 'Parent and child share pages until one writes; only then is the page copied, reducing fork() overhead', 'Copies before reading', 'No optimization exists'], correct: 1, explanation: 'COW allows parent and child processes (after fork) to share the same physical pages. Pages are marked read-only; when either process writes, a page fault triggers copying only that page, greatly reducing fork overhead.' },
  { question: 'What is the difference between hard link and soft link (symbolic link)?', options: ['Same thing', 'Hard link: direct inode reference, survives file deletion; Soft link: path reference, breaks if target deleted', 'Soft link references inode', 'Hard link breaks when target deleted'], correct: 1, explanation: 'Hard link is another directory entry pointing to the same inode — file exists until all hard links are removed. Soft/symbolic link stores the path to target — becomes a dangling link if target is deleted.' },
  { question: 'What is the inode structure in Unix file systems?', options: ['File name storage', 'Data structure storing file metadata (permissions, size, timestamps, block pointers) but not the file name', 'Directory entry', 'Process structure'], correct: 1, explanation: 'An inode stores file metadata: permissions, owner, size, timestamps, and pointers to data blocks. The file name is stored in the directory entry which maps names to inode numbers.' },
  { question: 'What is the difference between interrupt-driven I/O and DMA?', options: ['Same mechanism', 'Interrupt-driven: CPU handles each byte/word transfer; DMA: dedicated controller transfers blocks directly to memory', 'DMA uses CPU for every byte', 'Interrupt-driven is faster for large transfers'], correct: 1, explanation: 'Interrupt-driven I/O: CPU is interrupted for each word transferred (inefficient for large data). DMA (Direct Memory Access): a DMA controller transfers entire blocks between device and memory without CPU involvement, freeing CPU for other work.' },
  { question: 'What is a spinlock and when is it appropriate to use?', options: ['Lock that rotates', 'Lock where thread continuously checks (busy-waits) until available; appropriate for short critical sections on multiprocessor systems', 'Always appropriate', 'Same as mutex'], correct: 1, explanation: 'A spinlock busy-waits (loops checking lock availability) instead of sleeping. Appropriate for very short critical sections on multiprocessor systems where context switch overhead would exceed wait time. Wasteful on single-core.' },
  { question: 'What is the difference between process scheduling and thread scheduling?', options: ['Same scheduling', 'Process scheduling: OS selects which process runs; Thread scheduling: can be user-level (process manages) or kernel-level (OS manages)', 'Thread scheduling is simpler', 'Process scheduling handles threads'], correct: 1, explanation: 'Process scheduling selects which process gets CPU time. Thread scheduling can happen at user level (runtime library schedules within process) or kernel level (OS directly schedules threads across cores).' },
  { question: 'What is the difference between swapping and paging?', options: ['Same mechanism', 'Swapping moves entire process between memory and disk; Paging moves individual pages as needed', 'Paging moves entire process', 'Swapping moves individual pages'], correct: 1, explanation: 'Swapping moves an entire process between main memory and backing store (disk). Paging is finer-grained — individual pages are moved between memory and disk as needed, allowing partial loading.' },
  { question: 'What is the difference between starvation and deadlock?', options: ['Same condition', 'Starvation: process waits indefinitely due to priority; Deadlock: processes circularly wait for each other\'s resources', 'Deadlock is a type of starvation', 'Starvation involves circular wait'], correct: 1, explanation: 'Starvation: a process never gets resources because other higher-priority processes keep getting them (can be resolved by aging). Deadlock: circular dependency where no process can proceed (requires breaking a condition).' },
  { question: 'What is the role of the Memory Management Unit (MMU)?', options: ['CPU cache', 'Hardware component that translates virtual addresses to physical addresses and enforces memory protection', 'Disk controller', 'Network interface'], correct: 1, explanation: 'MMU translates virtual addresses to physical addresses using page tables, checks access permissions (read/write/execute), generates page faults for non-resident pages, and implements memory protection between processes.' },
  { question: 'What is the difference between blocking and non-blocking I/O?', options: ['Same behavior', 'Blocking: process waits until I/O completes; Non-blocking: returns immediately, process can do other work', 'Non-blocking always waits', 'Blocking never waits'], correct: 1, explanation: 'Blocking I/O: the calling process/thread is suspended until the I/O operation completes. Non-blocking I/O: the call returns immediately with whatever data is available (or an error), allowing the process to continue.' },
  { question: 'What is the Completely Fair Scheduler (CFS) in Linux?', options: ['Round Robin variant', 'Scheduler using red-black tree to track virtual runtime, ensuring fair CPU time distribution among processes', 'Priority-based only', 'FCFS implementation'], correct: 1, explanation: 'CFS uses a red-black tree sorted by virtual runtime (how much CPU time a process has received). It always picks the process with the smallest virtual runtime, ensuring proportionally fair CPU allocation based on priorities.' },
  { question: 'What is the difference between real-time OS and general-purpose OS?', options: ['Same thing', 'RTOS guarantees deterministic response times for time-critical tasks; general-purpose OS optimizes throughput and fairness', 'General-purpose has faster response', 'RTOS is always faster'], correct: 1, explanation: 'RTOS provides guaranteed worst-case response times (critical for medical devices, flight controls). General-purpose OS (Windows, Linux) optimizes for overall throughput and user experience without strict timing guarantees.' },
  { question: 'What is the difference between multiprocessing and multithreading?', options: ['Same concept', 'Multiprocessing uses multiple CPUs/cores for processes; Multithreading runs multiple threads within a single process', 'Multithreading uses multiple CPUs', 'Multiprocessing is within one process'], correct: 1, explanation: 'Multiprocessing: multiple processes run on multiple CPUs/cores (true parallelism, separate memory). Multithreading: multiple threads within one process share memory (concurrency, lighter weight).' },
  { question: 'What is demand paging and how does the page fault handler work?', options: ['Loading all pages at start', 'Pages loaded only when accessed; page fault triggers: save state, find frame, read page from disk, update page table, resume', 'Pages loaded randomly', 'No fault handling needed'], correct: 1, explanation: 'Demand paging loads pages only when accessed. On page fault: 1) Trap to OS, 2) Save process state, 3) Find free frame (or replace one), 4) Read page from disk, 5) Update page table, 6) Restart instruction.' },
  { question: 'What is the difference between process creation using fork() and exec()?', options: ['Same function', 'fork() creates child process (copy of parent); exec() replaces current process image with new program', 'exec() creates child', 'fork() replaces program'], correct: 1, explanation: 'fork() creates a new child process that is a copy of the parent (with COW optimization). exec() replaces the current process\'s memory space with a new program. Often used together: fork() then exec() to run a new program.' },
  { question: 'What is the difference between a monitor and a semaphore?', options: ['Same mechanism', 'Monitor: high-level construct with built-in mutual exclusion and condition variables; Semaphore: low-level counter-based primitive', 'Semaphore is higher-level', 'Monitor uses counters'], correct: 1, explanation: 'Monitors are high-level synchronization constructs (like Java synchronized methods) providing automatic mutual exclusion with condition variables for signaling. Semaphores are lower-level requiring careful programming to avoid errors.' },
  { question: 'What is memory protection and how does the OS enforce it?', options: ['Antivirus', 'Preventing processes from accessing each other\'s memory; enforced through base/limit registers, page table permissions, and hardware support', 'Encryption only', 'Cannot be enforced'], correct: 1, explanation: 'Memory protection prevents one process from corrupting another\'s memory. Enforced via: base/limit registers, page table permission bits (read/write/execute), privilege levels (user/kernel mode), and hardware MMU validation.' },
  { question: 'What is the difference between symmetric and asymmetric multiprocessing?', options: ['Same architecture', 'SMP: all processors are equal and share OS; AMP: one master processor controls, others execute tasks', 'AMP is more common', 'SMP has a master'], correct: 1, explanation: 'SMP: all processors are peers, run the same OS, share memory and I/O. More common in modern systems. AMP: one master processor manages system, assigns tasks to slave processors. Simpler but less flexible.' },
  { question: 'What is the buddy system in memory allocation?', options: ['Sharing memory with friends', 'Divides memory into power-of-2 blocks; splits and merges buddies to satisfy and free requests efficiently', 'Random allocation', 'Stack-based allocation'], correct: 1, explanation: 'Buddy system allocates memory in power-of-2 sized blocks. To allocate: split larger blocks until appropriate size found. To free: merge with buddy block if it\'s also free. Reduces external fragmentation, enables fast coalescing.' },
  { question: 'What is the role of the bootloader in the OS startup process?', options: ['Runs applications', 'First program that runs at power-on; loads OS kernel from disk into memory and transfers control to it', 'Manages files', 'Network initialization'], correct: 1, explanation: 'Bootloader (GRUB, UEFI) is loaded by BIOS/UEFI from disk\'s boot sector. It initializes minimal hardware, loads the OS kernel into memory, passes boot parameters, and transfers control to the kernel\'s entry point.' },
  { question: 'What are signals in Unix/Linux and how are they used?', options: ['Radio signals', 'Software interrupts sent to processes for asynchronous notification of events; e.g., SIGKILL, SIGTERM, SIGINT', 'Network packets', 'System calls'], correct: 1, explanation: 'Signals are asynchronous notifications sent to processes. SIGKILL (force kill), SIGTERM (graceful termination), SIGINT (Ctrl+C), SIGSEGV (segfault). Processes can catch/handle most signals except SIGKILL and SIGSTOP.' },
  { question: 'What is the difference between a file descriptor and an inode?', options: ['Same thing', 'File descriptor: per-process integer referencing an open file; Inode: on-disk structure storing file metadata', 'Inode is per-process', 'File descriptor stores metadata'], correct: 1, explanation: 'File descriptor is a per-process integer that references an entry in the system-wide open file table (created on open()). Inode is an on-disk data structure storing file metadata (size, permissions, block pointers) — not the name.' },
  { question: 'What is the slab allocator in Linux?', options: ['Memory defragmenter', 'Memory allocation mechanism that caches frequently used kernel objects to reduce allocation overhead', 'Disk allocator', 'Network buffer manager'], correct: 1, explanation: 'Slab allocator pre-allocates caches of frequently used kernel objects (inodes, task_structs). Objects are reused from the cache instead of repeatedly allocated/freed, reducing fragmentation and improving performance.' },
];
